<p id="notice"><%= notice %></p>
<!-- 投稿詳細 -->
<div class="container">
  <div class="row">
    <div class="col-xs-3">
    	<%= render "users/profile", user: @curent_user %>
        <%= render 'books/new', book: @book %>
      </div>
       <div class="col-xs-9">
<table class="table table-striped">

<h2> Book detail </h2>

<tr>
  <th><%= link_to attachment_image_tag(@user, :profile_image, :fill, 15, 15, fallback: "no_image.jpg", class: "img-circle pull-left profile-thumb"),user_path(@user)%><%= link_to @user.name, user_path(@user)%></th>
  <th><%= @book.title %></th>
  <th><%= @book.body %></th>
  <% if @user == current_user %>
  <th><%= link_to 'Edit', edit_book_path(@book)%> </th>
  <td><%= link_to 'Destroy', book_path(@book),method: :delete, data: { confirm: 'Are you sure?' } %></td>
  <% end %>
</tr>

</table>
</div>
</div>
</div>
